{
  "name": "@sagecards/shared-types",
  "version": "0.1.0",
  "private": true,
  "description": "Single source of truth for API contracts (Zod schemas + inferred types)",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@sagecards/tsconfig": "workspace:*",
    "typescript": "^5.0.0",
    "vitest": "^4.0.0"
  }
}
